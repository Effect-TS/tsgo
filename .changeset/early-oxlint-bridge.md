---
"@effect/tsgo": minor
---

Rename the public Go integration package from `etsapi` to `etsgoapi`, and add an experimental `etsjsapi` bridge that runs selected Effect diagnostics and suggestion-grade quick fixes synchronously against current-file overlays through `tsgo --effect-js-api`.
