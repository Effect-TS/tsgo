---
"@effect/tsgo": minor
---

Ship an Effect-aware Oxlint configuration schema and configure existing `.oxlintrc.json` files to use it when enabling the Oxlint integration. Keep the bundled TypeScript and Oxlint base schemas synchronized through the upstream update workflow.
