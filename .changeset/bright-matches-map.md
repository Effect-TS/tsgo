---
"@effect/tsgo": minor
---

Add the `matchEffectToMapBoth` style diagnostic and quick fix for replacing `Effect.matchEffect` handlers that return `Effect.fail` and `Effect.succeed` with `Effect.mapBoth`.
