---
"@effect/tsgo": minor
---

Add the `lazyEffect` diagnostic for exported zero-argument functions, exported interface members, and `Context.Service` members that lazily return `Effect` or `Stream` values.
