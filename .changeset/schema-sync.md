---
"@effect/tsgo": minor
---

Add the opt-in `schemaSync` diagnostic to prefer Effect-based Schema decoding and encoding over `decodeSync`, `decodeUnknownSync`, `encodeSync`, and `encodeUnknownSync` in any context. It recommends the corresponding Effect v3 or v4 method and is enabled by the `effect-native` preset.
