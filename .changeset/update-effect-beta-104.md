---
"@effect/tsgo": patch
---

Update Effect v4 dependencies and embedded test fixtures to `4.0.0-beta.104`, update Schema class completions for the `Error` and `TaggedError` renames, and suppress auto-imports from blocked Effect internal modules.
