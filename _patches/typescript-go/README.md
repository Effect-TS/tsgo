# TypeScript-Go patches

Store Effect-owned patches against the TypeScript-Go revision recorded in
`_packages/tsgo/upstream.json` here. `repoctl submodules setup` applies
`*.patch` files in bytewise filename order.
