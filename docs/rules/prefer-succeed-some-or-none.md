<!-- This file is generated by internal/rules/rules_json_test.go. Do not edit it manually. -->

# `preferSucceedSomeOrNone`

Suggests using Effect.succeedNone or Effect.succeedSome instead of wrapping Option.none or Option.some with Effect.succeed

| Property | Value |
| --- | --- |
| Category | Style |
| Default severity | `suggestion` |
| Fixable | Yes |
| Effect versions | v3, v4 |
| Diagnostic codes | `TS377117` |
| Language Service name | `preferSucceedSomeOrNone` |
| Oxlint name | `effecttsgo/prefer-succeed-some-or-none` |

## Preview

```ts
import { Effect, Option } from "effect"

export const none = Effect.succeed(Option.none())
/**
                    ^^^^^^^^^^^^^^ effecttsgo(prefer-succeed-some-or-none): `Effect.succeedNone` expresses this Option success value directly.
*/
export const some = Effect.succeed(Option.some(1))
/**
                    ^^^^^^^^^^^^^^ effecttsgo(prefer-succeed-some-or-none): `Effect.succeedSome` expresses this Option success value directly.
*/
```

## Language Service Configuration

See the [Language Service setup guide](../../README.md#installation) for installation instructions.

```jsonc
{
  "$schema": "./node_modules/@effect/tsgo/schema.json",
  "compilerOptions": {
    "plugins": [
      {
        "name": "@effect/language-service",
        "diagnosticSeverity": {
          "preferSucceedSomeOrNone": "warning"
        }
      }
    ]
  }
}
```

## Oxlint Configuration

See the [Oxlint setup guide](../README.md#oxlint-setup) for installation and patching instructions.

```json
{
  "$schema": "./node_modules/@effect/tsgo/oxlint-schema.json",
  "options": {
    "typeAware": true
  },
  "plugins": ["effecttsgo"],
  "rules": {
    "effecttsgo/prefer-succeed-some-or-none": "warn"
  }
}
```
