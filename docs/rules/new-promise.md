<!-- This file is generated by internal/rules/rules_json_test.go. Do not edit it manually. -->

# `newPromise`

Warns when constructing promises with new Promise instead of using Effect APIs

| Property | Value |
| --- | --- |
| Category | Effect-native |
| Default severity | `off` |
| Fixable | No |
| Effect versions | v3, v4 |
| Diagnostic codes | `TS377080` |
| Language Service name | `newPromise` |
| Oxlint name | `effecttsgo/new-promise` |

## Preview

```ts
export const preview = new Promise<number>((resolve) => resolve(1))
/**
                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ effecttsgo(new-promise): This code constructs `new Promise(...)`, prefer Effect APIs such as `Effect.async`, `Effect.promise`, or `Effect.tryPromise` instead of manual Promise construction.
*/
```

## Language Service Configuration

See the [Language Service setup guide](../../README.md#installation) for installation instructions.

```jsonc
{
  "compilerOptions": {
    "plugins": [
      {
        "name": "@effect/language-service",
        "diagnosticSeverity": {
          "newPromise": "warning"
        }
      }
    ]
  }
}
```

## Oxlint Configuration

See the [Oxlint setup guide](../README.md#oxlint-setup) for installation and patching instructions.

```json
{
  "plugins": ["effecttsgo"],
  "rules": {
    "effecttsgo/new-promise": "warn"
  }
}
```
