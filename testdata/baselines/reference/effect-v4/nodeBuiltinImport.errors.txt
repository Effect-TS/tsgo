=== Metadata ===
Effect version: 4.0.0

/.src/nodeBuiltinImport.ts(5,16): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(6,22): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(7,26): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(8,39): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(9,22): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(10,18): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(11,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(12,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(13,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(14,31): warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(15,22): warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect/unstable/process`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(16,23): warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect/unstable/process`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(17,18): warning TS377057: This module reference uses the `http` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(18,19): warning TS377057: This module reference uses the `https` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(19,25): warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(20,40): warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(21,28): warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(22,46): warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(23,37): warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(24,41): warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(25,26): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(26,42): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(27,26): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(28,42): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(29,32): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(30,54): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(31,25): warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(32,28): warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(35,8): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(38,21): warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(39,31): warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(40,30): warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(41,30): warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
/.src/nodeBuiltinImport.ts(42,30): warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "types": ["node"],
        "plugins": [
          {
            "name": "@effect/language-service",
            "ignoreEffectErrorsInTscExitCode": true,
            "skipDisabledOptimization": true
          }
        ]
      }
    }
    

==== /.src/nodeBuiltinImport.ts (34 errors) ====
    // @effect-diagnostics nodeBuiltinImport:warning
    import { pipe } from "effect"
    
    // Flagged: ES module imports for covered modules
    import fs from "fs"
                   ~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import * as fs2 from "node:fs"
                         ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { readFile } from "fs/promises"
                             ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { readFile as readFile2 } from "node:fs/promises"
                                          ~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    import { join } from "path"
                         ~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import path from "node:path"
                     ~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join2 } from "path/posix"
                                  ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join3 } from "node:path/posix"
                                  ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join4 } from "path/win32"
                                  ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { join as join5 } from "node:path/win32"
                                  ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `path` module, the corresponding Effect API is `Path` from `effect`. effect(nodeBuiltinImport)
    import { exec } from "child_process"
                         ~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect/unstable/process`. effect(nodeBuiltinImport)
    import { spawn } from "node:child_process"
                          ~~~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `child_process` module, the corresponding Effect API is `ChildProcess` from `effect/unstable/process`. effect(nodeBuiltinImport)
    import http from "http"
                     ~~~~~~
!!! warning TS377057: This module reference uses the `http` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
    import https from "node:https"
                      ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `https` module, the corresponding Effect API is `HttpClient` from `effect/unstable/http`. effect(nodeBuiltinImport)
    import { Console } from "console"
                            ~~~~~~~~~
!!! warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
    import { Console as NodeConsole } from "node:console"
                                           ~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
    import { setTimeout } from "timers"
                               ~~~~~~~~
!!! warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
    import { setTimeout as nodeSetTimeout } from "node:timers"
                                                 ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
    import { setTimeout as delay } from "timers/promises"
                                        ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
    import { setTimeout as nodeDelay } from "node:timers/promises"
                                            ~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
    import { Readable } from "stream"
                             ~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import { Readable as NodeReadable } from "node:stream"
                                             ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import { pipeline } from "stream/promises"
                             ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import { pipeline as nodePipeline } from "node:stream/promises"
                                             ~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import { ReadableStream } from "stream/web"
                                   ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import { ReadableStream as NodeReadableStream } from "node:stream/web"
                                                         ~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    import * as crypto from "crypto"
                            ~~~~~~~~
!!! warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)
    import { randomUUID } from "node:crypto"
                               ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)
    
    // Flagged: side-effect import
    import "node:fs"
           ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    
    // Flagged: top-level require
    const fs3 = require("node:fs")
                        ~~~~~~~~~
!!! warning TS377057: This module reference uses the `fs` module, the corresponding Effect API is `FileSystem` from `effect`. effect(nodeBuiltinImport)
    const consoleModule = require("console")
                                  ~~~~~~~~~
!!! warning TS377057: This module reference uses the `console` module, the corresponding Effect API is `Console` from `effect`. effect(nodeBuiltinImport)
    const timersModule = require("node:timers/promises")
                                 ~~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `timers` module, the corresponding Effect API is `Effect` from `effect`. effect(nodeBuiltinImport)
    const streamModule = require("stream/web")
                                 ~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `stream` module, the corresponding Effect API is `Stream` from `effect`. effect(nodeBuiltinImport)
    const cryptoModule = require("node:crypto")
                                 ~~~~~~~~~~~~~
!!! warning TS377057: This module reference uses the `crypto` module, the corresponding Effect API is `Crypto` from `effect`. effect(nodeBuiltinImport)
    
    // Not flagged: only explicitly covered subpaths match
    import { text } from "node:stream/consumers"
    const unknownTimersSubpath = require("timers/promises/extra")
    
    // Not flagged: Effect-native imports
    // @ts-expect-error - @effect/platform not installed in harness
    import { FileSystem } from "@effect/platform"
    
    // Not flagged: third-party modules with similar names
    // @ts-expect-error - fs-extra not installed in harness
    import fsExtra from "fs-extra"
    
