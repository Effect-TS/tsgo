=== Quick Fix Inventory ===

[D1] (9:41-9:53) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D2] (13:3-13:15) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D3] (16:3-16:15) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D4] (21:3-21:15) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D5] (27:44-27:56) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D6] (28:26-28:38) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D7] (29:25-29:37) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

[D8] (34:4-34:16) TS377115: `Effect.orDie` expresses escalating every typed failure into a defect more directly than `Effect.catch` with an identity-forwarding `Effect.die` handler. effect(catchAllDieToOrDie)
  Fix 0: "Disable catchAllDieToOrDie for this line"
  Fix 1: "Disable catchAllDieToOrDie for entire file"
  Fix 2: "Replace with Effect.orDie"

=== Quick Fix Application Results ===

=== [D1] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D1] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D1] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.orDie)

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D2] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D2] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D2] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.orDie
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D3] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D3] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D3] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.orDie
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D4] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D4] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D4] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.orDie
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D5] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D5] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D5] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.orDie)
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D6] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D6] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D6] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.orDie(failable)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D7] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D7] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D7] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.orDie(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.catch(Effect.die))

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))


=== [D8] Fix 0: "Disable catchAllDieToOrDie for this line" ===
skipped by default

=== [D8] Fix 1: "Disable catchAllDieToOrDie for entire file" ===
skipped by default

=== [D8] Fix 2: "Replace with Effect.orDie" ===

--- file:///.src/catchAllDieToOrDie.ts ---
// @effect-v4
// @effect-diagnostics *:off
// @effect-diagnostics catchAllDieToOrDie:warning
import { Effect, pipe } from "effect"

declare const failable: Effect.Effect<number, Error>

// Should trigger: direct API reference.
export const pipeDirect = failable.pipe(Effect.catch(Effect.die))

// Should trigger: concise and block identity-forwarding functions.
export const pipeArrow = failable.pipe(
  Effect.catch((error) => Effect.die(error))
)
export const pipeArrowBlock = failable.pipe(
  Effect.catch((error) => {
    return Effect.die(error)
  })
)
export const pipeFunction = failable.pipe(
  Effect.catch(function(error) {
    return Effect.die(error)
  })
)

// Should trigger: Function.pipe and both dual application forms.
export const functionPipe = pipe(failable, Effect.catch(Effect.die))
export const dataFirst = Effect.catch(failable, Effect.die)
export const dataLast = Effect.catch(Effect.die)(failable)

// Should trigger: Effect.fn trailing transformations are piping flows too.
export const effectFn = Effect.fn(function*() {
  return yield* failable
}, Effect.orDie)

// Should not trigger: the failure is transformed before becoming a defect.
export const wrapped = failable.pipe(
  Effect.catch((error) => Effect.die(new Error(String(error))))
)

// Should not trigger: this is not identity forwarding.
export const differentValue = failable.pipe(
  Effect.catch((_error) => Effect.die("different"))
)

// Should not trigger: Effect.orDie would itself be redundant.
export const unfailable = Effect.succeed(1).pipe(
  Effect.catch(Effect.die)
)

// Should not trigger: partial catches are not equivalent to Effect.orDie.
class TaggedError {
  readonly _tag = "TaggedError"
}
declare const tagged: Effect.Effect<number, TaggedError>
export const partial = tagged.pipe(
  Effect.catchTag("TaggedError", Effect.die)
)

// Should not trigger: a local API with the same member names.
const LocalEffect = {
  catch: (_handler: (error: Error) => Effect.Effect<never>) =>
    <A, E, R>(self: Effect.Effect<A, E, R>) => self,
  die: Effect.die
}
export const localApi = failable.pipe(LocalEffect.catch(LocalEffect.die))

