=== Quick Fix Inventory ===

[D1] (10:45-10:59) TS377124: `Effect.provideService` provides this inline single-service layer directly. effect(provideLayerSucceedToProvideService)
  Fix 0: "Disable provideLayerSucceedToProvideService for this line"
  Fix 1: "Disable provideLayerSucceedToProvideService for entire file"
  Fix 2: "Replace with Effect.provideService"

[D2] (11:49-11:63) TS377124: `Effect.provideServiceEffect` provides this inline single-service layer directly. effect(provideLayerSucceedToProvideService)
  Fix 0: "Disable provideLayerSucceedToProvideService for this line"
  Fix 1: "Disable provideLayerSucceedToProvideService for entire file"
  Fix 2: "Replace with Effect.provideServiceEffect"

[D3] (12:33-12:47) TS377124: `Effect.provideService` provides this inline single-service layer directly. effect(provideLayerSucceedToProvideService)
  Fix 0: "Disable provideLayerSucceedToProvideService for this line"
  Fix 1: "Disable provideLayerSucceedToProvideService for entire file"
  Fix 2: "Replace with Effect.provideService"

[D4] (13:30-13:44) TS377124: `Effect.provideServiceEffect` provides this inline single-service layer directly. effect(provideLayerSucceedToProvideService)
  Fix 0: "Disable provideLayerSucceedToProvideService for this line"
  Fix 1: "Disable provideLayerSucceedToProvideService for entire file"
  Fix 2: "Replace with Effect.provideServiceEffect"

[D5] (17:3-17:17) TS377124: `Effect.provideService` provides this inline single-service layer directly. effect(provideLayerSucceedToProvideService)
  Fix 0: "Disable provideLayerSucceedToProvideService for this line"
  Fix 1: "Disable provideLayerSucceedToProvideService for entire file"
  Fix 2: "Replace with Effect.provideService"

=== Quick Fix Application Results ===

=== [D1] Fix 0: "Disable provideLayerSucceedToProvideService for this line" ===
skipped by default

=== [D1] Fix 1: "Disable provideLayerSucceedToProvideService for entire file" ===
skipped by default

=== [D1] Fix 2: "Replace with Effect.provideService" ===

--- file:///.src/provideLayerSucceedToProvideService.ts ---
import { Context, Effect, Layer, pipe } from "effect"

class Service extends Context.Service<Service, { readonly value: number }>()("Service") {}

const implementation = { value: 1 }
const acquisition = Effect.succeed(implementation)
const program = Service.use((service) => Effect.succeed(service.value))

// Should trigger: pipeable, function-pipe, data-first, and curried layer constructors.
export const pipeableSucceed = program.pipe(Effect.provideService(Service, implementation))
export const functionPipeEffect = pipe(program, Effect.provide(Layer.effect(Service, acquisition)))
export const dataFirstSucceed = Effect.provide(program, Layer.succeed(Service)(implementation))
export const curriedEffect = Effect.provide(Layer.effect(Service)(acquisition))(program)

// Should preserve the arguments' multiline shape in the replacement.
export const multiline = program.pipe(
  Effect.provide(Layer.succeed(
    Service,
    {
      value: 2
    }
  ))
)

// Should not trigger: named layers may intentionally be shared and memoized.
const sharedLayer = Layer.succeed(Service, implementation)
export const namedLayer = program.pipe(Effect.provide(sharedLayer))

// Should not trigger: layer-to-layer provision is a different operation.
export const layerProvision = Layer.empty.pipe(Layer.provide(Layer.succeed(Service, implementation)))

// Should not trigger: unrelated APIs with the same method names.
const unrelated = {
  provide: (value: unknown) => value,
  succeed: (...args: ReadonlyArray<unknown>) => args
}
export const unrelatedCall = unrelated.provide(unrelated.succeed(Service, implementation))


=== [D2] Fix 0: "Disable provideLayerSucceedToProvideService for this line" ===
skipped by default

=== [D2] Fix 1: "Disable provideLayerSucceedToProvideService for entire file" ===
skipped by default

=== [D2] Fix 2: "Replace with Effect.provideServiceEffect" ===

--- file:///.src/provideLayerSucceedToProvideService.ts ---
import { Context, Effect, Layer, pipe } from "effect"

class Service extends Context.Service<Service, { readonly value: number }>()("Service") {}

const implementation = { value: 1 }
const acquisition = Effect.succeed(implementation)
const program = Service.use((service) => Effect.succeed(service.value))

// Should trigger: pipeable, function-pipe, data-first, and curried layer constructors.
export const pipeableSucceed = program.pipe(Effect.provide(Layer.succeed(Service, implementation)))
export const functionPipeEffect = pipe(program, Effect.provideServiceEffect(Service, acquisition))
export const dataFirstSucceed = Effect.provide(program, Layer.succeed(Service)(implementation))
export const curriedEffect = Effect.provide(Layer.effect(Service)(acquisition))(program)

// Should preserve the arguments' multiline shape in the replacement.
export const multiline = program.pipe(
  Effect.provide(Layer.succeed(
    Service,
    {
      value: 2
    }
  ))
)

// Should not trigger: named layers may intentionally be shared and memoized.
const sharedLayer = Layer.succeed(Service, implementation)
export const namedLayer = program.pipe(Effect.provide(sharedLayer))

// Should not trigger: layer-to-layer provision is a different operation.
export const layerProvision = Layer.empty.pipe(Layer.provide(Layer.succeed(Service, implementation)))

// Should not trigger: unrelated APIs with the same method names.
const unrelated = {
  provide: (value: unknown) => value,
  succeed: (...args: ReadonlyArray<unknown>) => args
}
export const unrelatedCall = unrelated.provide(unrelated.succeed(Service, implementation))


=== [D3] Fix 0: "Disable provideLayerSucceedToProvideService for this line" ===
skipped by default

=== [D3] Fix 1: "Disable provideLayerSucceedToProvideService for entire file" ===
skipped by default

=== [D3] Fix 2: "Replace with Effect.provideService" ===

--- file:///.src/provideLayerSucceedToProvideService.ts ---
import { Context, Effect, Layer, pipe } from "effect"

class Service extends Context.Service<Service, { readonly value: number }>()("Service") {}

const implementation = { value: 1 }
const acquisition = Effect.succeed(implementation)
const program = Service.use((service) => Effect.succeed(service.value))

// Should trigger: pipeable, function-pipe, data-first, and curried layer constructors.
export const pipeableSucceed = program.pipe(Effect.provide(Layer.succeed(Service, implementation)))
export const functionPipeEffect = pipe(program, Effect.provide(Layer.effect(Service, acquisition)))
export const dataFirstSucceed = Effect.provideService(program, Service, implementation)
export const curriedEffect = Effect.provide(Layer.effect(Service)(acquisition))(program)

// Should preserve the arguments' multiline shape in the replacement.
export const multiline = program.pipe(
  Effect.provide(Layer.succeed(
    Service,
    {
      value: 2
    }
  ))
)

// Should not trigger: named layers may intentionally be shared and memoized.
const sharedLayer = Layer.succeed(Service, implementation)
export const namedLayer = program.pipe(Effect.provide(sharedLayer))

// Should not trigger: layer-to-layer provision is a different operation.
export const layerProvision = Layer.empty.pipe(Layer.provide(Layer.succeed(Service, implementation)))

// Should not trigger: unrelated APIs with the same method names.
const unrelated = {
  provide: (value: unknown) => value,
  succeed: (...args: ReadonlyArray<unknown>) => args
}
export const unrelatedCall = unrelated.provide(unrelated.succeed(Service, implementation))


=== [D4] Fix 0: "Disable provideLayerSucceedToProvideService for this line" ===
skipped by default

=== [D4] Fix 1: "Disable provideLayerSucceedToProvideService for entire file" ===
skipped by default

=== [D4] Fix 2: "Replace with Effect.provideServiceEffect" ===

--- file:///.src/provideLayerSucceedToProvideService.ts ---
import { Context, Effect, Layer, pipe } from "effect"

class Service extends Context.Service<Service, { readonly value: number }>()("Service") {}

const implementation = { value: 1 }
const acquisition = Effect.succeed(implementation)
const program = Service.use((service) => Effect.succeed(service.value))

// Should trigger: pipeable, function-pipe, data-first, and curried layer constructors.
export const pipeableSucceed = program.pipe(Effect.provide(Layer.succeed(Service, implementation)))
export const functionPipeEffect = pipe(program, Effect.provide(Layer.effect(Service, acquisition)))
export const dataFirstSucceed = Effect.provide(program, Layer.succeed(Service)(implementation))
export const curriedEffect = Effect.provideServiceEffect(Service, acquisition)(program)

// Should preserve the arguments' multiline shape in the replacement.
export const multiline = program.pipe(
  Effect.provide(Layer.succeed(
    Service,
    {
      value: 2
    }
  ))
)

// Should not trigger: named layers may intentionally be shared and memoized.
const sharedLayer = Layer.succeed(Service, implementation)
export const namedLayer = program.pipe(Effect.provide(sharedLayer))

// Should not trigger: layer-to-layer provision is a different operation.
export const layerProvision = Layer.empty.pipe(Layer.provide(Layer.succeed(Service, implementation)))

// Should not trigger: unrelated APIs with the same method names.
const unrelated = {
  provide: (value: unknown) => value,
  succeed: (...args: ReadonlyArray<unknown>) => args
}
export const unrelatedCall = unrelated.provide(unrelated.succeed(Service, implementation))


=== [D5] Fix 0: "Disable provideLayerSucceedToProvideService for this line" ===
skipped by default

=== [D5] Fix 1: "Disable provideLayerSucceedToProvideService for entire file" ===
skipped by default

=== [D5] Fix 2: "Replace with Effect.provideService" ===

--- file:///.src/provideLayerSucceedToProvideService.ts ---
import { Context, Effect, Layer, pipe } from "effect"

class Service extends Context.Service<Service, { readonly value: number }>()("Service") {}

const implementation = { value: 1 }
const acquisition = Effect.succeed(implementation)
const program = Service.use((service) => Effect.succeed(service.value))

// Should trigger: pipeable, function-pipe, data-first, and curried layer constructors.
export const pipeableSucceed = program.pipe(Effect.provide(Layer.succeed(Service, implementation)))
export const functionPipeEffect = pipe(program, Effect.provide(Layer.effect(Service, acquisition)))
export const dataFirstSucceed = Effect.provide(program, Layer.succeed(Service)(implementation))
export const curriedEffect = Effect.provide(Layer.effect(Service)(acquisition))(program)

// Should preserve the arguments' multiline shape in the replacement.
export const multiline = program.pipe(
  Effect.provideService(Service, {
    value: 2
})
)

// Should not trigger: named layers may intentionally be shared and memoized.
const sharedLayer = Layer.succeed(Service, implementation)
export const namedLayer = program.pipe(Effect.provide(sharedLayer))

// Should not trigger: layer-to-layer provision is a different operation.
export const layerProvision = Layer.empty.pipe(Layer.provide(Layer.succeed(Service, implementation)))

// Should not trigger: unrelated APIs with the same method names.
const unrelated = {
  provide: (value: unknown) => value,
  succeed: (...args: ReadonlyArray<unknown>) => args
}
export const unrelatedCall = unrelated.provide(unrelated.succeed(Service, implementation))

