=== Metadata ===
Effect version: 4.0.0

/.src/acquireReleaseDisposable_preview.ts(9,25): warning TS377119: `Effect.acquireDisposable` expresses this disposable resource acquisition more directly than `Effect.acquireRelease` with a manual disposal finalizer. effect(acquireReleaseDisposable)


==== /.src/acquireReleaseDisposable_preview.ts (1 errors) ====
    // @effect-v4
    // @effect-diagnostics *:off
    // @effect-diagnostics acquireReleaseDisposable:warning
    /// <reference lib="esnext.disposable" />
    import { Effect } from "effect"
    
    declare const acquire: Effect.Effect<AsyncDisposable>
    
    export const resource = Effect.acquireRelease(
                            ~~~~~~~~~~~~~~~~~~~~~
!!! warning TS377119: `Effect.acquireDisposable` expresses this disposable resource acquisition more directly than `Effect.acquireRelease` with a manual disposal finalizer. effect(acquireReleaseDisposable)
      acquire,
      (resource) => Effect.promise(() => resource[Symbol.asyncDispose]())
    )
    
