=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_siblingDeclarators.ts(4,3): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_siblingDeclarators.ts(10,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_siblingDeclarators.ts(19,3): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_siblingDeclarators.ts (3 errors) ====
    import { Effect } from "effect"
    
    export const limit = 10,
      f = (n: number) =>
      ~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
        Effect.gen(function* () {
          return (yield* Effect.succeed(n)) + limit
        })
    
    // Preserve a sibling after the target, including its comment and literal spelling.
    export const first = (n: number) =>
                 ~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
      Effect.gen(function* () {
        return (yield* Effect.succeed(n)) + trailing
      }),
      // trailing sibling
      trailing = 1_000
    
    // Preserve both sides of a target in the middle of a declaration list.
    export let leading = 0xff,
      middle = function(n: number) {
      ~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... })`. effect(effectFnOpportunity)
        return Effect.gen(function* () {
          return (yield* Effect.succeed(n)) + leading + last
        })
      },
      last = 2_000
    
