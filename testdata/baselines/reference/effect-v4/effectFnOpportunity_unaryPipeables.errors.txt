=== Metadata ===
Effect version: 4.0.0

/.src/effectFnOpportunity_unaryPipeables.ts(4,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(name, attributes) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(5,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_unaryPipeables.ts(11,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("mixed")(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(23,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(24,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_unaryPipeables.ts(28,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(29,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_unaryPipeables.ts(35,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(36,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_unaryPipeables.ts(41,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(51,14): suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(name) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
/.src/effectFnOpportunity_unaryPipeables.ts(52,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
/.src/effectFnOpportunity_unaryPipeables.ts(59,3): suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)


==== /.src/tsconfig.json (0 errors) ====
    {
      "compilerOptions": {
        "plugins": [
          {
            "name": "@effect/language-service",
            "effectFn": ["span", "suggested-span", "inferred-span", "no-span", "untraced"]
          }
        ]
      }
    }
    

==== /.src/effectFnOpportunity_unaryPipeables.ts (13 errors) ====
    import { Effect } from "effect"
    
    // Issue #758, point 5: ignore must only receive the Effect, not name/attributes.
    export const record = (name: string, attributes: Record<string, string>) =>
                 ~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(name, attributes) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(name, attributes)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(Effect.ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    const ignore = Effect.ignore
    
    export const mixed = (n: number) =>
                 ~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fn("mixed")(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
        const value = yield* Effect.succeed(n)
        return value
      }).pipe(
        Effect.map((value) => value + 1_000),
        // keep this comment with the bare identifier
        ignore, // keep the trailing comment too
        Effect.withSpan("mixed"),
      )
    
    // Non-call expressions need parentheses when used as the wrapper's callee.
    export const conditional = (n: number) =>
                 ~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(Math.random() > 0.5 ? Effect.ignore : ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    export const callback = (n: number) =>
                 ~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe((effect) => Effect.ignore(effect))
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    // Do not shadow an existing underscore reference in the pipe argument.
    const _ = Effect
    export const underscore = (n: number) =>
                 ~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(n)
    ~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(_.ignore)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    // Parentheses around an existing factory call do not require a wrapper.
    export const factory = (n: number) =>
                 ~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(n) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
        const value = yield* Effect.succeed(n)
        return value
      }).pipe((Effect.map((value) => value + 1)))
    
    // Model pipeables whose optional second parameter must not receive name.
    const withOptionalOptions = (effect: Effect.Effect<void>, options?: { readonly silent: boolean }) =>
      options?.silent ? Effect.ignore(effect) : effect
    
    export const optionalOptions = (name: string) =>
                 ~~~~~~~~~~~~~~~
!!! suggestion TS377047: This expression can be rewritten in the reusable function form `Effect.fnUntraced(function*(name) { ... }, ...pipeTransformations)`. effect(effectFnOpportunity)
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(name)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(withOptionalOptions)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
    // A spread is not a single callable expression; do not offer the gen conversion.
    const operators = [Effect.ignore] as const
    export const spread = (name: string) =>
      Effect.gen(function* () {
      ~~~~~~~~~~~~~~~~~~~~~~~~~
        yield* Effect.log(name)
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
      }).pipe(...operators)
    ~~~~
!!! suggestion TS377017: This Effect.gen contains a single return statement. effect(unnecessaryEffectGen)
    
