=== Metadata ===
Effect version: 4.0.0

/.src/flatMapConditionalToFilterOrFail_preview.ts(5,1): error TS377001: This Effect value is neither yielded nor used in an assignment. effect(floatingEffect)
/.src/flatMapConditionalToFilterOrFail_preview.ts(6,3): suggestion TS377119: `Effect.filterOrFail` expresses this conditional validation more directly than `Effect.flatMap` with an identity `Effect.succeed` branch. effect(flatMapConditionalToFilterOrFail)


==== /.src/flatMapConditionalToFilterOrFail_preview.ts (2 errors) ====
    import { Effect } from "effect"
    
    declare const program: Effect.Effect<number>
    
    program.pipe(
    ~~~~~~~~~~~~~
      Effect.flatMap((value) =>
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~
      ~~~~~~~~~~~~~~
!!! suggestion TS377119: `Effect.filterOrFail` expresses this conditional validation more directly than `Effect.flatMap` with an identity `Effect.succeed` branch. effect(flatMapConditionalToFilterOrFail)
        value > 0 ? Effect.succeed(value) : Effect.fail("non-positive")
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      )
    ~~~
    )
    ~
!!! error TS377001: This Effect value is neither yielded nor used in an assignment. effect(floatingEffect)
    
