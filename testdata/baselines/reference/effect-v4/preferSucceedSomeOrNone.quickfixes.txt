=== Quick Fix Inventory ===

[D1] (5:34-5:48) TS377117: `Effect.succeedNone` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedNone"

[D2] (8:34-8:48) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D3] (11:37-11:47) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D4] (14:39-14:53) TS377117: `Effect.succeedNone` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedNone"

[D5] (15:39-15:53) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D6] (18:43-18:57) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D7] (21:46-21:60) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D8] (24:57-24:71) TS377117: `Effect.succeedNone` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedNone"

[D9] (25:58-25:72) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D10] (28:66-28:80) TS377117: `Effect.succeedNone` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedNone"

[D11] (29:67-29:81) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

[D12] (32:69-32:83) TS377117: `Effect.succeedSome` expresses this Option success value directly. effect(preferSucceedSomeOrNone)
  Fix 0: "Disable preferSucceedSomeOrNone for this line"
  Fix 1: "Disable preferSucceedSomeOrNone for entire file"
  Fix 2: "Replace with Effect.succeedSome"

=== Quick Fix Application Results ===

=== [D1] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D1] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D1] Fix 2: "Replace with Effect.succeedNone" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeedNone

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D2] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D2] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D2] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeedSome(1)

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D3] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D3] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D3] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeedSome("value")

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D4] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D4] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D4] Fix 2: "Replace with Effect.succeedNone" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeedNone
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D5] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D5] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D5] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeedSome(1)

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D6] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D6] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D6] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeedSome(1)

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D7] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D7] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D7] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeedSome<number>(1)

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D8] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D8] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D8] Fix 2: "Replace with Effect.succeedNone" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Effect.succeedNone
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D9] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D9] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D9] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Effect.succeedSome(1)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D10] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D10] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D10] Fix 2: "Replace with Effect.succeedNone" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = Effect.succeedNone
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D11] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D11] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D11] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = Effect.succeedSome(1)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = pipe(1, Option.some, Effect.succeed)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))


=== [D12] Fix 0: "Disable preferSucceedSomeOrNone for this line" ===
skipped by default

=== [D12] Fix 1: "Disable preferSucceedSomeOrNone for entire file" ===
skipped by default

=== [D12] Fix 2: "Replace with Effect.succeedSome" ===

--- file:///.src/preferSucceedSomeOrNone.ts ---
import { Effect, Effect as Fx, Option, Option as O, pipe } from "effect"
import { none as optionNone, some as optionSome } from "effect/Option"

// Should trigger: direct None constructor
export const shouldTriggerNone = Effect.succeed(Option.none())

// Should trigger: direct Some constructor
export const shouldTriggerSome = Effect.succeed(Option.some(1))

// Should trigger: aliased modules
export const shouldTriggerAliases = Fx.succeed(O.some("value"))

// Should trigger: named Option exports
export const shouldTriggerNamedNone = Effect.succeed(optionNone())
export const shouldTriggerNamedSome = Effect.succeed(optionSome(1))

// Should trigger: parenthesized Option call
export const shouldTriggerParenthesized = Effect.succeed((Option.some(1)))

// Should trigger: preserve an explicit Some type argument in the fix
export const shouldTriggerSomeTypeArgument = Effect.succeed(Option.some<number>(1))

// Should trigger: method-pipe forms
export const shouldTriggerNonePipe = Option.none().pipe(Effect.succeed)
export const shouldTriggerSomePipe = Option.some(1).pipe(Effect.succeed)

// Should trigger: Function.pipe forms
export const shouldTriggerNoneFunctionPipe = pipe(Option.none(), Effect.succeed)
export const shouldTriggerSomeFunctionPipe = pipe(Option.some(1), Effect.succeed)

// Should trigger: adjacent transformations in an existing flow
export const shouldTriggerTransformationPair = Effect.succeedSome(1)

// Should NOT trigger: an explicit None type argument cannot be preserved by Effect.succeedNone
export const shouldNotTriggerNoneTypeArgument = Effect.succeed(Option.none<number>())

// Should NOT trigger: an explicit outer type argument can intentionally widen the success type
export const shouldNotTriggerOuterTypeArgument = Effect.succeed<Option.Option<number>>(Option.none())

// Should NOT trigger: already using the direct constructors
export const shouldNotTriggerSucceedNone = Effect.succeedNone
export const shouldNotTriggerSucceedSome = Effect.succeedSome(1)

// Should NOT trigger: the Option functions are values rather than calls
export const shouldNotTriggerNoneFunction = Effect.succeed(Option.none)
export const shouldNotTriggerSomeFunction = Effect.succeed(Option.some)

const LocalEffect = {
  succeed: <A>(value: A) => Effect.succeed(value)
}
const LocalOption = {
  none: () => Option.none(),
  some: <A>(value: A) => Option.some(value)
}

// Should NOT trigger: look-alike APIs do not resolve to Effect's exports
export const shouldNotTriggerLocalEffect = LocalEffect.succeed(Option.none())
export const shouldNotTriggerLocalOptionNone = Effect.succeed(LocalOption.none())
export const shouldNotTriggerLocalOptionSome = Effect.succeed(LocalOption.some(1))

