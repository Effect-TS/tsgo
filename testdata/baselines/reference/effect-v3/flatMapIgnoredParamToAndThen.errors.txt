=== Metadata ===
Effect version: 3.19.19

/.src/flatMapIgnoredParamToAndThen.ts(10,36): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(13,34): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(16,26): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(19,25): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(23,35): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(39,37): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
/.src/flatMapIgnoredParamToAndThen.ts(45,21): suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)


==== /.src/flatMapIgnoredParamToAndThen.ts (7 errors) ====
    import * as Fx from "effect/Effect"
    import { Effect, pipe } from "effect"
    
    const first = Effect.succeed(1)
    const second = Effect.succeed("second")
    const holder = { second }
    let mutable = second
    
    // Should trigger: pipeable style
    export const pipeable = first.pipe(Effect.flatMap(() => second))
                                       ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    
    // Should trigger: pipe style
    export const piped = pipe(first, Effect.flatMap(() => second))
                                     ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    
    // Should trigger: data-first style
    export const dataFirst = Effect.flatMap(first, () => second)
                             ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    
    // Should trigger: data-last style
    export const dataLast = Effect.flatMap(() => second)(first)
                            ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    
    // Should trigger and fix the call site without changing the shared alias
    const flatMap = Fx.flatMap
    export const aliased = first.pipe(flatMap(() => second))
                                      ~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    
    // Should NOT trigger: the callback consumes or declares an upstream value
    export const parameter = first.pipe(Effect.flatMap((_value) => second))
    
    // Should NOT trigger: moving these evaluations would change laziness
    // @effect-diagnostics-next-line flatMapToMap:off
    export const call = first.pipe(Effect.flatMap(() => Effect.succeed("new")))
    export const propertyAccess = first.pipe(Effect.flatMap(() => holder.second))
    
    // Should NOT trigger: the returned binding is mutable or declared later
    export const mutableBinding = first.pipe(Effect.flatMap(() => mutable))
    export const laterBinding = first.pipe(Effect.flatMap(() => later))
    const later = Effect.succeed("later")
    
    // Should trigger: the lazy-expression parser unwraps a single-return block
    export const blockBody = first.pipe(Effect.flatMap(() => {
                                        ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
      return second
    }))
    
    // Should trigger: const values remain stable when referenced from a nested scope
    export function nestedScope() {
      return first.pipe(Effect.flatMap(() => second))
                        ~~~~~~~~~~~~~~
!!! suggestion TS377132: `Effect.andThen` expresses this sequencing more directly than `Effect.flatMap` with a zero-parameter callback. effect(flatMapIgnoredParamToAndThen)
    }
    
    // Should NOT trigger: the stable value is not an Effect
    const value = "value"
    export const nonEffect = first.pipe(Effect.flatMap(() => value as never))
    
    // Should NOT trigger: unrelated flatMap API
    const unrelated = { flatMap: <A, B>(_input: A, f: () => B): B => f() }
    export const unrelatedCall = unrelated.flatMap(1, () => second)
    
    void mutable
    
